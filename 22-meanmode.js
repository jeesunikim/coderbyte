function MeanMode(arr) { 
  //mode is the repeated ones
  //mean is average
  var largest=arr.pop();
  var mean=0;
  var average;
  for(var i=0;i<arr.length;i++){ //find average;
  if(arr[i]);
  mean+=arr[i];
  average=mean/arr.length;
  }
  if(mode === average){
    return 1;
  }else{return 0;}        
}
   
// keep this function call here 
// to see how to enter arguments in JavaScript scroll down
MeanMode(readline()); 